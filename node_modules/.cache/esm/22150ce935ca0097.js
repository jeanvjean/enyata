let logger;_35c‍.x([["sequelize",()=>sequelize],["default",()=>_35c‍.o]]);_35c‍.w("../utils/logger",[["default",["logger"],function(v){logger=v}]]);const Sequelize = require('sequelize');

const detectPort = require('detect-port');


const connection = (app, port) => 
    new Promise(async resolve=>{
        port = port.port || (await detectPort());

        const server = app.listen(port,()=>{
            logger.log(`Listening On port: ${port}`);
        });

        resolve(server);
    });

       const  sequelize = new Sequelize('enyata', 'postgres', `jeanvaljean`, {
    host:'localhost',
    dialect:"postgres",
    port: 9900
});

_35c‍.d(connection);